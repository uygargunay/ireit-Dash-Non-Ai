services:
  irei-mvp:
    build: .
    ports:
      - "5078:8080"
    environment:
      Irei__AdminKey: "${IREI_ADMIN_KEY:?Set IREI_ADMIN_KEY before starting}"
    volumes:
      - irei-submissions:/app/App_Data

volumes:
  irei-submissions:
